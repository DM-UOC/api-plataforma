import { UsuarioValidacionModel } from './usuario.validacion.model';

describe('UsuarioValidacion.Model', () => {
  it('should be defined', () => {
    expect(new UsuarioValidacionModel()).toBeDefined();
  });
});
