import { MateriaProfesorModel } from './materia.profesor.model';

describe('MateriaProfesorModel', () => {
  it('should be defined', () => {
    expect(new MateriaProfesorModel()).toBeDefined();
  });
});
